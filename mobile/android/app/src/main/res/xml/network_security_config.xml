<?xml version="1.0" encoding="utf-8"?>
<!--
  Depuis Android 9, le HTTP en clair est refusé par défaut. Le serveur de
  développement Django écoute en HTTP simple : sans cette exception,
  l'application ne joindrait pas l'API sur un réseau local.

  L'exception est volontairement limitée aux adresses de développement —
  `10.0.2.2` est l'alias de la machine hôte vu depuis l'émulateur Android.
  Un déploiement réel doit passer par HTTPS ; ajoutez alors le domaine de
  production ici uniquement s'il sert encore du HTTP, ce qui n'est pas
  souhaitable pour des données scolaires.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
